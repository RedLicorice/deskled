# Flash dumps can contain Wi-Fi credentials
backup/

# PlatformIO build output
firmware/.pio/

# Firmware signing: the private key must never be published; the header is generated from public.key at build time
firmware/keys/private.key
firmware/include/signing_pubkey.h

# stray capture files
*.log
